# Project rules
* project version is 0.0.1
* use Go programming language
* build project for Linux platform (Arch linux specifically but try to use distro-agnostic tools, so it could be executed on most of linux distributions)
* update progress.md after each chnage in code base, keep tracking of token used / model name in the end of progress.md (accumulate it as project develops)
* progress.md includes only feature name from files in 'features' directory and short desription, base on feature details
* use Binance API to get candlesticks trading data
