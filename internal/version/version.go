// Package version holds the application identity shown in the UI.
package version

// Name is the application name displayed in the header.
const Name = "TradeMan"

// Version is the project version. Keep it in sync with context.md.
const Version = "0.0.1"
